#include "index.h"

void
apply(vm_t *vm, value_t *target, size_t arity) {
    (void) vm;
    (void) target;
    (void) arity;
    // switch (def->kind) {
    // case DEF_FUNCTION: {
    //     apply_function(vm, def->function);
    //     return;
    // }

    // case DEF_NODE: {
    //     apply_node(vm, def->node, arity);
    //     return;
    // }
    // }
}
