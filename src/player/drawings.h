#pragma once

void draw_background(player_t *self, canvas_t *canvas, bool with_grid);
void draw_net(player_t *self, canvas_t *canvas);
void draw_toolbar(player_t *self, canvas_t *canvas);
void draw_cursor(player_t *self, canvas_t *canvas);
